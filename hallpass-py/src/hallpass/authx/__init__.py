"""The authentication helpers integrations share: JWT signing (RS256,
PS256), OAuth 2.0 client credentials and JWT bearer exchanges, a token
cache, AWS SigV4, STS and ambient credentials, and Google service accounts.

Nothing here is vendor specific; each integration composes what it needs.
"""
